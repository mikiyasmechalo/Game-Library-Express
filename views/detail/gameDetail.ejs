<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Game Details</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-dark text-white p-5">
  <div class="container-fluid">
    <a href="/" class="btn btn-info mb-4">Back to Inventory</a>
    <h1 class="text-center mb-4 text-info">
      <%= game.title %>
    </h1>
    <hr class="border-info mb-4">

    <div class="row justify-content-center">
      <div class="col-lg-8">
        <div class="card bg-secondary text-white">
          <div class="card-body">
            <h5 class="card-title">Description</h5>
            <p class="card-text">
              <%= game.description %>
            </p>
            <ul class="list-group list-group-flush bg-secondary">
              <li class="list-group-item bg-secondary text-white">
                <strong>Release Date:</strong>
                <%= new Date(game.release_date).toLocaleDateString() %>
              </li>
              <li class="list-group-item bg-secondary text-white">
                <strong>Rating:</strong>
                <%= game.rating %> out of 5
              </li>
              <li class="list-group-item bg-secondary text-white">
                <strong>Genres:</strong>
                <% game.genres.forEach((genre, index)=> { %>
                  <a href="/genre/<%= genre.id %>" class="badge bg-info text-decoration-none me-1 text-black">
                    <%= genre.name %>
                  </a>
                  <%= index < game.genres.length - 1 ? '' : '' %>
                    <% }); %>
              </li>
              <li class="list-group-item bg-secondary text-white">
                <strong>Publishers:</strong>
                <% game.publishers.forEach((publisher, index)=> { %>
                  <a href="/publisher/<%= publisher.id %>" class="badge bg-info text-decoration-none me-1 text-black">
                    <%= publisher.name %>
                  </a>
                  <%= index < game.publishers.length - 1 ? '' : '' %>
                    <% }); %>
              </li>
            </ul>
          </div>
        </div>
        <div class="mt-3">
          <a href="/game/update/<%=game.id%>" class="btn btn-info mb-2">Update Game</a>
          <form action="/game/delete/<%=game.id%>" method="post" class="delete-form ">
            <button type="submit" class="btn btn-danger"
              onclick="return confirm('Are you sure you want to delete this item?');">
              Delete Game
            </button>
          </form>
        </div>
      </div>

    </div>
  </div>
  <!-- Bootstrap Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>