<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Publisher Details</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .delete-form {
      display: inline;
    }

    .delete-button {
      background-color: #dc3545;
      color: white;
      border: 1px solid #dc3545;
      border-radius: 0.25rem;
      text-decoration: none;
      cursor: pointer;
      font-size: 1em;
      padding: 0.375rem 0.75rem;
      margin: 0;
      font-family: inherit;
    }

    .delete-button:hover {
      background-color: #c82333;
      border-color: #bd2130;
    }
  </style>
</head>

<body class="bg-dark text-white p-5">
  <div class="container-fluid">
    <a href="/" class="btn btn-info mb-4">Back to Inventory</a>
    <h1 class="text-center mb-4 text-info">
      <%= publisher.name %>
    </h1>
    <hr class="border-info mb-4">

    <div class="row justify-content-center">
      <div class="col-lg-8">
        <div class="card bg-secondary text-white mb-4">
          <div class="card-body">
            <h5 class="card-title">Description</h5>
            <p class="card-text">
              <%= publisher.description %>
            </p>
          </div>
        </div>

        <h5 class="mt-4">Games from this publisher:</h5>
        <% if (publisher.games && publisher.games.length> 0 && publisher.games[0].id) { %>
          <ul class="list-group list-group-flush">
            <% publisher.games.forEach(game=> { %>
              <li class="list-group-item bg-secondary text-white">
                <a href="/game/details?id=<%= game.id %>" class="text-info text-decoration-none">
                  <%= game.title %>
                </a>
              </li>
              <% }); %>
          </ul>
          <% } else { %>
            <div class="alert alert-warning">No games found for this publisher.</div>
            <% } %>
              <div class="mt-3">
                <a href="/publisher/update/<%=publisher.id%>" class="btn btn-info">Update Publisher</a>
                <form action="/publisher/delete/<%=publisher.id%>" method="post" class="delete-form ">
                  <button type="submit" class="btn btn-danger"
                    onclick="return confirm('Are you sure you want to delete this item?');">
                    Delete Publisher
                  </button>
                </form>
              </div>
      </div>
    </div>
  </div>
  <!-- Bootstrap Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>